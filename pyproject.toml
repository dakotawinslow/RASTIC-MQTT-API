[project]
name = "rastic-mqtt-api"
version = "0.1.0"
description = "Bridges MQTT topics to a RESTful HTTPS API"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=46.0.5",
    "flask>=3.1.3",
    "paho-mqtt>=2.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
