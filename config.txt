[mqtt]
# MQTT broker hostname or IP address
host = 172.16.0.100

# MQTT broker port (standard: 1883, TLS: 8883)
port = 1883

# Credentials — leave blank if the broker allows anonymous access
username =
password =

# Set to true to enable TLS/SSL on the MQTT connection
use_tls = false

# Path to a CA certificate file for broker TLS verification.
# Leave blank to use system default CA bundle.
ca_cert =

# Client ID presented to the MQTT broker. Leave blank to let the library
# generate a unique ID automatically.
client_id =

# Keepalive interval in seconds
keepalive = 60


[api]
# Network interface to bind the REST API server to.
# Use 0.0.0.0 to accept connections on all interfaces,
# or 127.0.0.1 to restrict to localhost only.
host = 0.0.0.0

# HTTPS port. Port 443 requires elevated privileges on Linux/macOS.
# Use a high port (e.g. 8443) to run without sudo.
port = 8443

# Set to false to serve over plain HTTP instead of HTTPS.
# Useful for development or when TLS is handled by a reverse proxy.
use_ssl = false

# TLS certificate and private key for HTTPS.
# If these files do not exist, a self-signed certificate is generated
# automatically (requires the 'cryptography' package).
ssl_cert = cert.pem
ssl_key  = key.pem


[widget]
# Comma-separated MQTT topics to display in the /widget dashboard.
# Optionally append :Friendly Name after each topic.
# Leave blank to show an empty dashboard.
topics = door/main/status:Front Door, resources/Mac-Mini:Mac Mini, resources/Playground-VM:Playground, resources/RASTIC-XPS: XPS, resources/Zyxel-AP:Rastic WiFi,
